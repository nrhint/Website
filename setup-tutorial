This is the help file and a tutorial file to use this edditor.

with this program you can use a template to create blank pages for your site.  To create a new page you create a new file then coose the template file.

USING HTML:

EDDITING:
	The html menu contains many common tags for usage.  To insert these tags you just click on the menu then the item that you want to insert.  

	To save a file click the File menu then click save.  This will save your file.  To exit the file you click Exit in the same menu.  THis will save and exit. You can only edit one file at a time.
